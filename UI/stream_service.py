class StreamServiceIntegration:
    def start_stream(self, platform, key):
        print(f"Starting stream to {platform} with key {key}.")

    def stop_stream(self):
        print("Stream stopped.")
